# 설정하기 — 순서대로 4번만 하면 끝납니다

이 문서만 따라가면 됩니다. 어려운 설명은 없고, **누를 버튼 이름**만 적었습니다.
막히면 **보이는 화면의 메뉴 이름을 그대로 알려주세요.** 제가 맞춰서 다시 안내합니다.

> 지금 상태가 궁금할 때는 배포 후 `내주소/admin/setup` 에 들어가면
> 무엇이 끝났고(✅) 무엇이 남았는지(❌) 화면이 알려줍니다.

---

## 1단계. GitHub에서 Merge 버튼 누르기

**왜 하나요?** 자동 실행(하루 3회)은 저장소의 **기본 브랜치**에서만 동작합니다.
지금 코드는 별도 브랜치에 있으니 기본 브랜치로 합쳐야 합니다.

1. <https://github.com/jangkeeseoup1739-afk/beullogeu/pulls> 를 엽니다.
2. 목록에 있는 Pull request 제목을 클릭합니다.
3. 아래로 내려가 초록색 **Merge pull request** 를 누릅니다.
4. 이어서 나오는 **Confirm merge** 를 누릅니다.

**버튼이 안 보이거나 회색이면** 화면에 적힌 문장을 그대로 알려주세요.

---

## 2단계. Vercel에 올리고 값 2개 넣기

**왜 하나요?** 관리자 화면(글 확인·승인)을 쓰려면 인터넷에 올려야 합니다.

### 2-1. 저장소 올리기

1. <https://vercel.com/new> 접속 (GitHub 계정으로 로그인)
2. 목록에서 **beullogeu** 찾아 **Import** 클릭
3. 설정은 건드리지 말고 **Deploy** 클릭 → 1~2분 기다립니다

### 2-2. 값 2개 넣기

1. 배포가 끝나면 프로젝트 화면 위쪽 **Settings** 클릭
2. 왼쪽 메뉴에서 **Environment Variables** 클릭
3. 아래 두 개를 하나씩 추가합니다 (Key 칸에 이름, Value 칸에 값 → **Save**)

| Key (이름) | Value (값) |
| --- | --- |
| `ANTHROPIC_API_KEY` | <https://console.anthropic.com> → API keys 에서 발급한 `sk-ant-...` 값 |
| `ADMIN_PASSWORD` | 관리자 화면에 들어갈 때 쓸 비밀번호 (직접 정하세요. 잊지 않게 메모) |

### 2-3. 저장할 곳 만들기

1. 위쪽 **Storage** 탭 클릭
2. **Create Database** → **Blob** 선택 → **Create**
3. 이 프로젝트에 **Connect** (`BLOB_READ_WRITE_TOKEN` 이 자동으로 들어갑니다)

### 2-4. 다시 배포

1. 위쪽 **Deployments** 탭 클릭
2. 맨 위 줄 오른쪽 **⋯** → **Redeploy** → **Redeploy**

**주의:** 값을 넣은 뒤 Redeploy를 하지 않으면 적용되지 않습니다.

---

## 3단계. 글이 만들어지는지 확인하기

1. 브라우저에서 `내주소/admin` 으로 들어갑니다.
   (내주소는 Vercel 프로젝트 맨 위에 있는 `....vercel.app` 주소입니다)
2. 2단계에서 정한 **비밀번호**로 로그인합니다.
3. **새 글 만들기** 칸에서
   - 시간대: `09:00 뉴스·정책`
   - **미리보기만 (게시하지 않음)** 체크 유지
   - **미리보기 생성** 클릭
4. 1~3분 기다리면 아래에 Claude가 만든 부동산 글이 나타납니다.

여기까지 되면 **"매일 자동으로 부동산 글이 만들어지는 부분"이 완성**된 것입니다.
Threads에 올리는 것만 남았습니다.

**아무것도 안 나오면** 화면에 뜬 빨간 문구를 그대로 알려주세요.

---

## 4단계. Threads에 자동으로 올리기 (준비되면)

이 단계를 하기 전까지 글은 **승인 대기** 상태로만 쌓입니다. 사고가 날 일은 없습니다.

### 4-1. Threads 토큰 발급

1. <https://developers.facebook.com/apps> → **앱 만들기**
2. 사용 사례에서 **Threads** 관련 항목 선택 → 앱 이름 입력 → 만들기
3. 앱 화면에서 Threads 계정을 연결합니다
4. 권한에 `threads_basic`, `threads_content_publish` 를 추가합니다
5. **액세스 토큰 생성** → 나온 긴 문자열을 복사합니다
6. 브라우저 주소창에 아래를 붙여 열고 `id` 숫자를 적어둡니다

```
https://graph.threads.net/v1.0/me?fields=id,username&access_token=복사한토큰
```

### 4-2. Vercel에 넣기

**Settings → Environment Variables** 에 추가 후 **Redeploy**

| Key | Value |
| --- | --- |
| `THREADS_ACCESS_TOKEN` | 복사한 토큰 |
| `THREADS_USER_ID` | 위에서 확인한 `id` 숫자 |

### 4-3. GitHub에도 넣기 (자동 실행용)

자동 실행은 GitHub에서 돌기 때문에 값이 따로 필요합니다.

저장소 → **Settings** → 왼쪽 **Secrets and variables** → **Actions**
→ **New repository secret** 으로 5개를 하나씩 등록합니다.

| Name | Secret |
| --- | --- |
| `ANTHROPIC_API_KEY` | Vercel에 넣은 것과 같은 값 |
| `ADMIN_PASSWORD` | Vercel에 넣은 것과 같은 값 |
| `BLOB_READ_WRITE_TOKEN` | Vercel → Storage → Blob 스토어 → `.env.local` 탭에서 복사 |
| `THREADS_ACCESS_TOKEN` | 4-1에서 발급한 토큰 |
| `THREADS_USER_ID` | 4-1에서 확인한 id 숫자 |

### 4-4. 시험 실행

1. 저장소 → **Actions** 탭
2. 왼쪽에서 **부동산 Threads 자동 게시** 클릭
3. 오른쪽 **Run workflow** → `dry_run` 을 **true** 로 두고 실행 (게시 안 함)
4. 잘 되면 `dry_run` **false** 로 한 번 더 실행해 실제 게시를 확인합니다

이후에는 매일 한국시간 **09:00 / 13:00 / 19:00** 에 알아서 돌아갑니다.

---

## 평소 관리

- 글 확인·승인: `내주소/admin`
- 청약·분양·세금·대출·제이원플렉스 글은 **승인 대기**로 올라옵니다.
  숫자와 일정을 확인한 뒤 **즉시 게시** 를 누르세요.
- 잠시 멈추고 싶으면 관리자 화면의 **자동화 ON → 끄기**
- 60일마다 Threads 토큰을 새로 받아야 합니다. 만료 14일 전부터 화면에 경고가 뜹니다.

## 자주 막히는 곳

| 증상 | 이렇게 하세요 |
| --- | --- |
| 로그인이 안 됨 | `ADMIN_PASSWORD` 를 넣고 **Redeploy** 했는지 확인 |
| "환경 변수가 없다"고 나옴 | 값을 넣은 뒤 Redeploy를 안 한 경우입니다 |
| 글이 안 만들어짐 | `ANTHROPIC_API_KEY` 가 맞는지, Anthropic 계정에 잔액이 있는지 확인 |
| 자동 실행이 안 됨 | 1단계 Merge를 했는지, GitHub Secrets 5개를 넣었는지 확인 |
| 기록이 사라짐 | Blob 스토어를 만들었는지 확인 (2-3단계) |

## 화면에서 직접 확인하기

`내주소/admin/setup` 에 들어가면 위 4단계가 각각 ✅ / ❌ 로 표시되고,
❌ 인 항목의 방법이 펼쳐져 나옵니다. 지금 무엇을 해야 하는지 한 줄로도 알려줍니다.
